import React from "react";
import "./Footer.css";

const Footer = () => {
  const handleOKButton = () => {
    //mutation to write data to db
  }

  return (
      <footer>
        <button type="submit">
          OK
        </button>
        <button type="cancel">
          Cancel
        </button>
    </footer>
  );
};

export default Footer;