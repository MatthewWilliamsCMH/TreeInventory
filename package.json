{
  "name": "summit-chase-tree-inventory",
  "version": "1.0.0",
  "scripts": {
    "install": "cd server && npm i && cd ../client && npm i",
    "dev": "concurrently \"cd server && npm run watch\" \"cd client && npm run dev\"",
    "build": "cd client && npm install && npm run build",
    "build:demo": "cd client && npm install && npm run build:demo -- --mode=demo",
    "build:production": "cd client && npm install && npm run build:production -- --mode=production",
    "start": "cd server && node server.js",
    "start:demo": "cd server && node server.js",
    "start:production": "cd server && node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "concurrently": "^9.1.0",
    "jsonwebtoken": "^9.0.2",
    "react-jwt": "^1.3.0"
  },
  "devDependencies": {
    "@vitest/browser-playwright": "^4.0.8",
    "@vitest/coverage-v8": "^4.0.8",
    "playwright": "^1.56.1",
    "prop-types": "^15.8.1",
    "vitest": "^4.0.8"
  }
}
